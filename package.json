{
  "name": "gsc-coverage-monitor",
  "version": "2.0.0",
  "description": "Search Console indexing-health monitor: detects vanished pages, impression collapses, index-state flips and sitemap failures across many properties — built only on GSC endpoints that actually return data",
  "type": "module",
  "bin": { "gsc-monitor": "src/index.js" },
  "main": "src/index.js",
  "scripts": {
    "poll": "node --no-warnings=ExperimentalWarning src/index.js poll",
    "report": "node --no-warnings=ExperimentalWarning src/index.js report",
    "status": "node --no-warnings=ExperimentalWarning src/index.js status",
    "test": "node --no-warnings=ExperimentalWarning --test \"tests/**/*.test.js\"",
    "lint": "node --check src/index.js && node --check src/config.js && for f in src/lib/*.js; do node --check $f; done"
  },
  "engines": { "node": ">=22.13.0" },
  "dependencies": {
    "googleapis": "^144.0.0"
  },
  "keywords": ["google-search-console", "gsc", "seo", "indexing", "url-inspection", "monitoring", "sqlite", "nodejs", "agency", "webmaster-tools"],
  "author": "Mehran Moghadasi",
  "license": "MIT"
}
